export enum Routes {
  USERS = 'users',
  TABLE_VERSIONS = 'table-versions',
  RECOMMENDATION_VERSIONS = 'recommendation-versions',
  SAVED_ANSWERS = 'saved-answers'
}

export enum SavedAnswersRoutes {
  ID = 'id',
  USER_ID = 'user-id',
  TABLE_VERSION = 'table-version'
}
